import Foundation

enum Constants {
    /// Shared instruction appended to each model's system prompt.
    static let sharedGroupInstruction = "You are participating in a multi-agent conversation. Be concise, avoid repetition, and reference other agents when helpful."
}
